# Runtime artifacts
logs/
KILL_SWITCH

# Backtest raw data only (large). Results, journal, and health files are the
# system's durable memory — they MUST be committed so reclones don't lose work.
backtest/data/

# Secrets — never commit keys
.env
*.key

# Python
__pycache__/
*.pyc
.venv/
